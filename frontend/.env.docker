# Baked into the web image by production/docker-compose.yml (service: web).
# Same-origin: nginx on :5174 proxies /api/ and /hubs/ → api:8080.
VITE_API_URL=
VITE_HUB_URL=http://localhost:5024/hubs
VITE_SIGNALR_ENABLED=true
VITE_DEV_LOGIN_PASSWORD=Ags@Dev2026